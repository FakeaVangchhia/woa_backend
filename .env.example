# Environment mode: development or production
ENV=development

# The backend URL (used by the agent loop)
BACKEND_URL=http://127.0.0.1:8420

# Allowed CORS origins in production (comma-separated list)
# ALLOWED_ORIGINS=https://myfrontend.render.com,https://anotherorigin.com
ALLOWED_ORIGINS=
